spring.application.name=MyGreetingApp

# Same context path for Spring Boot run and Tomcat WAR.
server.servlet.context-path=/MyGreetingApp

# Using 8081 avoids conflict if Tomcat is already running on 8080.
server.port=8081
